import React from 'react';

const Tool = ({tool}) => {
    console.log(tool)
    const {name , price , availableQuantity ,miniQuantity,img , description} = tool
    return (
        <div>
            
        </div>
    );
};

export default Tool;